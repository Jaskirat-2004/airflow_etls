CREATE TABLE acd_call_interval_summary (
    report_date                                 DATE,
    interval_start                              TIMESTAMP,
    interval_end                                TIMESTAMP,
    process_name                                TEXT,
    campaign_name                               TEXT,
    queue_name                                  TEXT,
    total_received_calls                        INTEGER,
    total_connected_calls                       INTEGER,
    total_abandoned_calls_at_acd                INTEGER,
    total_connected_in_target                   INTEGER,
    total_connected_calls_at_acd_in_target      INTEGER,
    total_connected_after_target                INTEGER,
    total_connected_calls_at_acd_after_target   INTEGER,
    sla_pct                                     NUMERIC,
    answered_pct                                NUMERIC,
    total_abandoned_before_target               INTEGER,
    total_abandoned_calls_at_acd_in_target      INTEGER,
    total_abandoned_after_target                INTEGER,
    total_abandoned_calls_at_acd_after_target   INTEGER,
    abandoned_pct                               NUMERIC,
    abandoned_call_on_agent                     INTEGER,
    total_short_calls                           INTEGER,
    total_talk_time                             VARCHAR(12),
    total_acd_wait_time                         VARCHAR(12),
    acd_wait_time_in_queue                      VARCHAR(12),
    avg_talk_time                               VARCHAR(12),
    total_hold_duration                         VARCHAR(12),
    max_hold_duration                           VARCHAR(12),
    min_hold_duration                           VARCHAR(12),
    avg_hold_duration                           VARCHAR(12),
    total_hold_count                            INTEGER,
    oldest_call_waiting                         VARCHAR(12),
    total_wrap_time                             VARCHAR(12),
    total_handling_time                         VARCHAR(12),
    avg_handling_time                           VARCHAR(12)
);